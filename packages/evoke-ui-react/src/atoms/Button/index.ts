/**
 * Button Component
 * Primary UI component for user interaction with multiple variants and states
 */

export { Button, buttonVariants } from './Button';
export type { ButtonProps } from './Button';